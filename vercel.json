{
  "buildCommand": "pnpm install --frozen-lockfile && node -e \"const fs=require('fs'); const path=require('path'); let dir=process.cwd(); while(dir!==path.dirname(dir) && !fs.existsSync(path.join(dir,'scripts/vercel-build.cjs'))){ dir=path.dirname(dir); } if(fs.existsSync(path.join(dir,'scripts/vercel-build.cjs'))){ require(path.join(dir,'scripts/vercel-build.cjs')); } else { console.error('vercel-build.cjs not found'); process.exit(1); }\"",
  "outputDirectory": "public",
  "framework": null,
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/$1" },
    { "source": "/((?!_next|favicon\\.svg|opengraph\\.jpg|robots\\.txt).*)", "destination": "/index.html" }
  ],
  "headers": [
    {
      "source": "/assets/(.*)",
      "headers": [
        { "key": "Cache-Control", "value": "public, max-age=31536000, immutable" }
      ]
    },
    {
      "source": "/(.*)",
      "headers": [
        { "key": "X-Content-Type-Options", "value": "nosniff" },
        { "key": "X-Frame-Options", "value": "DENY" },
        { "key": "Referrer-Policy", "value": "strict-origin-when-cross-origin" }
      ]
    }
  ]
}
